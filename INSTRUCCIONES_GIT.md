# 📝 Instrucciones para Conectar con Git

## 🚀 Pasos para conectar este proyecto al repositorio

### 1. Inicializar Git (si no está inicializado)

```bash
cd ai-meeting-summary-service
git init
```

### 2. Agregar el repositorio remoto

```bash
git remote add origin https://github.com/Hernan56IK/ia-pi3-trasncribir.git
```

### 3. Crear la rama main

```bash
git branch -M main
```

### 4. Agregar todos los archivos

```bash
git add .
```

### 5. Hacer el primer commit

```bash
git commit -m "Initial commit: Servicio de IA para resúmenes de reuniones"
```

### 6. Subir al repositorio

```bash
git push -u origin main
```

## ✅ Verificación

Después de hacer push, verifica en GitHub que todos los archivos estén presentes.

## 📋 Comandos útiles

```bash
# Ver estado
git status

# Ver commits
git log

# Ver remoto
git remote -v
```

## 🔄 Para futuros cambios

```bash
git add .
git commit -m "Descripción del cambio"
git push
```



